# Only for a manual Compose deployment. Ansible generates /opt/homelab/teamspeak/.env
# with distinct random values on the first deployment.
TEAMSPEAK_DB_PASSWORD=replace-with-a-random-secret
MARIADB_ROOT_PASSWORD=replace-with-a-different-random-secret
